[package]
name = "task"
version = "0.1.0"
edition = "2021"

[dependencies]
# ВАЖЛИВО: додано "env" для підтримки змінних оточення в аргументах
clap = { version = "4.4", features = ["derive", "env"] }
config = "0.13"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"